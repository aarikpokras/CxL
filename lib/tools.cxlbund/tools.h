#include <iostream>
#include <string>
#include <vector>
#pragma once
std::string remins(std::string subject, std::string what, int times);
std::vector<std::string> split(std::string subject, std::string delim,
                               int times);
std::string reverse(std::string subject);
